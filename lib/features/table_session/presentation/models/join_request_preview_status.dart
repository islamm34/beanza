/// In-memory enum for the Table Join Approval UI prototype only.
/// Used for design evaluation without backend or database connections.
enum JoinRequestPreviewStatus {
  pending,
  approved,
  rejected,
  expired,
  cancelled,
}
